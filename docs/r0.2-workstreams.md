# R0.2 workstreams and ownership

**Updated:** 2026-09-20  
**Goal:** complete the first reproducible training release without hiding unfinished acceptance work

Ownership is explicit so work can proceed in parallel. The named owner is responsible for producing
the evidence or decision; Codex remains responsible for integration, review and keeping the release
requirements honest.

## User-owned

| Work | Deliverable | Status |
|---|---|---|
| Enrol the second home GPU machine | The second machine appears online in the `Home` group with actual capability and GPU health evidence | Ready when the machine is accessible |
| Select the first useful model and dataset | A short description of the real training goal and where its authorised data comes from | Pending; the bundled smoke workload does not block this choice |
| Witness the physical disconnect exercise | Disconnect the selected worker when prompted, then reconnect it after the lease/result checks | **Ready to run**; the runbook is at `docs/acceptance/r0.2/network-loss-exercise-runbook.md` (#58) and records the exact agent and soak-workload digests |

The user SHALL NOT need to edit database records, copy cloud credentials to a worker or manually
construct internal API requests for these tasks.

## Codex-owned

| Work | Deliverable | Status |
|---|---|---|
| Integrate and deploy parallel changes | Reviewed PRs, passing CI, deployment and live acceptance evidence | In progress |
| Durable output artefacts | Content-addressed output upload, integrity verification and completion acknowledgement | **Complete.** Agent delivery merged (#48, #49, #52); live acceptance passed on job `8bb95268-4906-45cc-b1a8-49908a3c861b`, with `model.pt` `VERIFIED` in GCS and a retained attempt resuming without rerunning the workload |
| Provenance and run visibility | Visible run identity, actual timing, image/data/config identity and honest unavailable estimates | **Complete.** Actual execution timing merged on both sides (#54, #56): the agent reports the container runtime's own interval, or nothing when there is no interval to report |
| Telemetry and MLflow | Stable job/attempt identifiers in OpenTelemetry and an associated MLflow run | **Unblocked and unstarted.** The artefact contract it waited on is complete. Identifier propagation and its correlation rules are settled (#32, #55, ADR-015 in #40) and a runnable MLflow is in place (#42, #57), so what remains is associating a job with an MLflow run |
| Network-loss safety | Automated coverage plus a live lease/replay exercise with no duplicate execution | Agent, database and server lease safeguards complete; the long workload (#43) and the runbook (#58) are both in place, so only the witnessed live exercise remains, and it is now user-owned |

## Delegated parallel work

| Workstream | Scope | Integration owner | Status |
|---|---|---|---|
| Reproducible GPU training workload | Offline immutable image, versioned bundled data, CUDA-only training and structured provenance | Codex | Complete; published immutable image succeeded through the production scheduler |
| Queued cancellation acceptance | Prove a cancelled queued job cannot be assigned on a later worker heartbeat | Codex | Complete; automated and live evidence recorded |
| First artefact-storage ADR | Decide the constrained-container output, agent collection, short-lived GCS upload and acknowledgement protocol | Codex | Complete; ADR-014 merged |
| Agent half of durable outputs | Verified manifests, authority through delivery, resumable uploads and cleanup obligations that survive a restart | Codex | Complete; #36, #48, #49, #52 merged and live |
| Heartbeats that survive slow work | The worker stays visible while a job runs and while a large image is pulled, and stops before creating a container if authority is lost | Codex | Complete; #37 and #60 merged, #60 live on THESHED2 |
| Job telemetry without job networking | Decide how a job with no network is correlated and observed | Codex | Complete; ADR-015 merged in #40 |
| Local observability stack | A runnable ADR-009 stack with bounded cardinality, real exemplars and a documented storage budget | Codex | Complete; #42 merged |
| Observability infrastructure | The same stack on GCP behind IAP, cost-gated, with a managed spend budget | Codex | **Merged in #57, not applied.** Configuration only; nothing is spending. The spend is approved at 300 in the billing account's currency, and the first apply needs a billing account id and an IAP OAuth client, then the three DNS records Terraform returns |
| Fixed-duration GPU soak workload | A workload long enough to disconnect a worker underneath | Codex | Complete; #43 merged |
| Workload authoring path | A copyable template, an authoring and publish guide, and operator fields for a custom image and output contract | Codex | Complete; #61 merged |

## Current release boundary

The production system can queue and execute an immutable GPU container, safely cancel queued and
active work, recover expired leases with a bounded retry, and record a real training run's exact
inputs, actual execution interval and durable verified outputs. That last part is no longer
outstanding: it passed live acceptance on 2026-09-20.

Two things stand between here and a complete R0.2:

1. **The witnessed network-loss exercise.** Everything it needs exists — the soak workload, the
   runbook and the digests it pins. It needs a human to disconnect a machine.
2. **A real model and dataset.** The bundled smoke and soak workloads prove the mechanism, not the
   purpose. R0.2 is a reproducible training release, and nothing is reproducing a training run
   anyone wanted yet.

Neither is blocked on engineering. Both are user-owned, which is worth stating plainly rather than
leaving them to look like work in progress.
